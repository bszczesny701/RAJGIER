services:
  - type: web
    name: raj-gier
    runtime: node
    plan: free
    buildCommand: npm install && npm install --include=dev --prefix client && npm install --prefix server && npm run build
    startCommand: npm start
    healthCheckPath: /health
